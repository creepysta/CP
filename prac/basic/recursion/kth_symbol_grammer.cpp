//https://leetcode.com/problems/k-th-symbol-in-grammar/

#include <bits/stdc++.h>
using namespace std;


int solve(int n, int k) {
    int p = 0;
    for (int i = 0; i < n; i++) {
        // choose
    }
}


int main() {
    int n, k;
    cin >> n >> k;
    int ans = solve(n, k);
    cout << ans << '\n';
    return 0;
}

/*
1)
    given N, A, B, T
    N number of cards in stack
    A at least 1/2 can pick cards
    B at most 1/2 can pick cards
    T == 1 ? 1 : 2 says who goes first
    if N < A, then the player with current turn, picks all cards
    find when 1 is the winner

2)
    2 people play the game
    A always goes first
    Box1 contains numbers from 1 to n inclusive.
    Every turn a player takes any number and puts it in Box2.
    If the sum of numbers in Box2 is a multiple of 3, the player with the current turn loses
    find if player A wins
*/

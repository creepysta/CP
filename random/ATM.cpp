#include <bits/stdc++.h>
using namespace std;

int main()
{
	int a[4];
	int max_notes, sum;
	cin >> max_notes >> sum;

	for(int i = 0; i < 4; i++) {
		cin >> a[i];
	}
	int total = 0;
	while(total != sum) {
		
	}

	return 0;
}

#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ui unsigned int

int main() {
	int t;
	cin >> t;
	while(t--) {
		int n, m;
		cin >> n >> m;
		vector<vector<ll>> a(n);
		for(int i = 0; i < n; i++) {
			a[i] = vector<int>(m);
			for(int j = 0; j < m; j++) {
				cin >> a[i][j];
			}
		}

		// apply dp
		vector<ll>
	return 0;
}
